# Pulse-Monitor

## Pulse Monitor Installation

1. Right-click "install.ps1"
2. Select "Run with PowerShell"
3. Click "Yes" when prompted for admin rights

That's it! The monitor will start automatically with Windows.

To uninstall, run "uninstall.ps1" the same way.
